name: latlong
description: Lightweight library for common latitude and longitude calculation
author: Mike Mitterer <office@mikemitterer.at>
homepage: https://github.com/MikeMitterer/dart-latlong

version: 0.6.1
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  validate:
    git: git@github.com:hendrikdruse/dart-validate.git

  intl: '>=0.15.0 <1.0.0'

dev_dependencies:
  test: any

  build_runner: any
  build_test: any
  build_web_compilers: any
